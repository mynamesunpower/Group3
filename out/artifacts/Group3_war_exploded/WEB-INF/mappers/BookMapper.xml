<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BookMapper">

    <!--도서정보입력-->
    <insert id="insertBook" parameterType="book">
        INSERT INTO BOOK(isbn, publisher_number, genre, total_page, price, title, stock, author, publication_date, content, keyword)
        VALUES(#{isbn}, #{publisherNumber}, #{genre}, #{totalPage}, #{price},#{title}, #{stock}, #{author}, #{publicationDate}, #{content}, #{keyword})
    </insert>

    <!--도서 리스트-->
    <select id="bookList" resultMap="getBook">
        	<![CDATA[
		SELECT * FROM book ORDER BY ISBN DESC
		]]>
    </select>

    <!--선택한 ISBN의 도서정보-->
    <select id="selectBook" resultType="book">
        	<![CDATA[
		SELECT * FROM book WHERE ISBN=#{isbn}
		]]>
    </select>

    <!--선택한 ISBN의 도서정보 삭제-->
    <delete id="deleteBook" parameterType="book">
         	<![CDATA[
		DELETE FROM book WHERE ISBN=#{isbn}
		]]>
    </delete>

    <!--선택한 ISBN의 도서정보 업데이트-->
    <update id="updateBook" parameterType="book">
       <![CDATA[
       UPDATE book SET PRICE= #{price} , CONTENT= #{content} WHERE ISBN= #{isbn}
        ]]>
    </update>

    <select id="searchBook" parameterType="hashMap" resultMap="getBook">
        SELECT * FROM BOOK
        <where>
            <!--
            title like #{keyword} or
            author like #{keyword} or
            genre like #{keyword} or
            keyword like #{keyword}-->
            <choose>
                <when test='sbox != null and sbox.equals("title")'>
                    title like #{keyword}
                </when>
                <when test='sbox != null and sbox.equals("author")'>
                    author like #{keyword}
                </when>
                <when test='sbox != null and sbox.equals("genre")'>
                    genre like #{keyword}
                </when>
                <when test='sbox != null and sbox.equals("keyword")'>
                    keyword like #{keyword}
                </when>
                <otherwise>
                    title like #{keyword} or author like #{keyword} or genre like #{keyword} or keyword like #{keyword}
                </otherwise>
            </choose>
        </where>
    </select>


    <resultMap id="getBook" type="book">
        <result property="isbn" column="isbn"/>
        <result property="publisherNumber" column="publisher_number"/>
        <result property="genre" column="genre"/>
        <result property="totalPage" column="total_page"/>
        <result property="price" column="price"/>
        <result property="title" column="title"/>
        <result property="stock" column="stock"/>
        <result property="author" column="author"/>
        <result property="publicationDate" column="publication_date"/>
        <result property="content" column="content"/>
        <result property="keyword" column="keyword"/>
    </resultMap>

</mapper>